[project]
name = "tack"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "run-in-rhino",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["tack", "tack.prompting"]

[tool.uv.sources]
run-in-rhino = { path = "../run-in-rhino", editable = true }
