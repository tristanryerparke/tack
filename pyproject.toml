[project]
name = "tack"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "rhino-python-renderer",
]

[tool.uv.sources]
rhino-python-renderer = { path = "../rhino-python-renderer", editable = true }
